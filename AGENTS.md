# Repository Guidelines

## 项目结构与模块组织
当前仓库为空，未发现源码、测试或资源目录。请在新增目录后在此补充说明。例如：`src/` 存放业务代码、`tests/` 存放测试、`assets/` 存放静态资源。若有多语言或多模块结构，建议在本节给出树状示例与模块职责边界。

## 构建、测试与本地开发命令
未检测到构建脚本（如 `package.json`、`Makefile` 或 `pyproject.toml`）。因此暂无官方命令可用。新增脚本后请在本节列出命令与目的，例如：`npm test`（运行单元测试）、`make build`（生成发布产物）。

## 代码风格与命名规范
当前未发现语言与格式化工具配置。新增代码时请明确缩进规则、命名模式和格式化工具，并保持全仓一致。例如：函数名是否使用 `camelCase`，文件名是否使用 `kebab-case`。如果引入自动格式化或 lint 工具，请注明配置文件路径。

## 测试指南
未发现测试框架或覆盖率要求。请在引入测试后补充：测试框架名称、覆盖率阈值、测试文件命名约定（例如 `*.spec.ts` 或 `test_*.py`），以及运行测试的命令。

## 提交与 Pull Request 规范
仓库未发现 `.git` 目录，无法总结历史提交规范。初始化版本控制后，请在此约定提交信息格式（例如 `feat: ...`、`fix: ...`）与 PR 要求（描述、关联问题、必要的截图或日志）。

## 安全与配置提示
如需密钥或环境变量，请使用示例文件（如 `.env.example`）并避免提交真实凭据。新增配置文件时，注明用途与加载方式。

## Agent 说明
本仓库当前以文档为主，若任务为规划或技术文档，优先更新文档而非源码。
